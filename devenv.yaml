# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  devenv-base:
    url: github:doot/devenv-base
    flake: true
  nixpkgs:
    follows: devenv-base/nixpkgs
  git-hooks:
    follows: devenv-base/git-hooks
imports:
  - devenv-base
profile: base
